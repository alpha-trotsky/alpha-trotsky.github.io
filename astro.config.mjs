import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // CHANGE THIS to your real URL once you know it.
  // If your repo is named "yourname.github.io", use: 'https://yourname.github.io'
  // If your repo has any other name (e.g. "site"), keep the base line below.
  site: 'https://alpha-trotsky.github.io',
  // base: '/site',   // <-- ONLY uncomment + set this if repo is NOT named yourname.github.io
});

